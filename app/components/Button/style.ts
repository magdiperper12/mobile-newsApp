import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
	container: { alignItems: 'center', padding: 30 },
	output: { fontWeight: 'bold', fontSize: 20, color: 'red' },
	button: {
		marginTop: 15,
		backgroundColor: '#0f0',
		padding: 10,
		borderRadius: 10,
		fontWeight: 'bold',
		color: '#fff',
	},
});

export default styles;
